# Mission Lifecycle Example

This example will demonstrate the governed flow:

```text
Goal or Plan -> Governed Mission -> Assignment -> Execution -> Observed Effect -> Recover or Replan
```

Mission dispatch is introduced only after the v1 contracts and reference adapter are published.
